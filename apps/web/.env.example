# GraphQL endpoint — leave blank to use the Vite dev proxy (/graphql → API_URL)
VITE_GRAPHQL_URL=

# API base URL used by the Vite dev proxy (not exposed to the browser)
API_URL=http://localhost:3000
